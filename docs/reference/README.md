# Reference

Facts, no narrative. For when you already understand it and need the formula.

* **[Cheat sheet](cheatsheet.md)** — the projection pipeline, both objects and
  every transformation rule, on one page you can print.
* **[Verification](verification.md)** — what this repository proves about its own
  numbers: agreement with OpenCV, byte-for-byte parity across the three ports,
  and why undistortion returns `NaN` instead of guessing.

Looking for the explanation rather than the formula? That is the
[course](../course/).
